= form_for @form do |f|
    .field
    = f.label :first_name
    = f.text_field :first_name

    .field
    = f.label :last_name
    = f.text_field :last_name

    .field
    = f.label :full_name
    = f.text_field :full_name

    .actions
        = f.submit 'Save'